query category($id: ID) {
  category(id: $id) {
    data {
      id
      attributes {
        Name
        products {
          data {
            ...ProductCard
          }
        }
      }
    }
  }
}
