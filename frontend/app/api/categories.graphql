query categories($filters: CategoryFiltersInput) {
  categories(filters: $filters) {
    data {
      id
      attributes {
        Name
        Slug
        products {
          data {
            ...ProductCard
          }
        }
      }
    }
  }
}
