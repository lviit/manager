schema:
  - http://localhost:1337/graphql:
      headers:
        Authorization: "Bearer: ${API_TOKEN}"
documents: 'app/api/**/*.graphql'
generates:
  app/api/generates.ts:
    plugins:
      - typescript
      - typescript-operations
